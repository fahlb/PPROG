compile with "gcc main.c -0 main -lm"
main will create a data file with position and velocity of the bodeis Saturn (0), Janus (1) and Epimetheus (2) over a time span of 10 years.

for resutls in terms of distance to center of mass as function of time compile results.c with "gcc results.c -o results -lm"
results will read the data file created by main and calculate the distance to the center of mass (cm) for each entrie. It then will write these results into a separate file.